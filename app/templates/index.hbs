<div class="row">
  <div class="col-sm-3 col-md-2 sidebar">
    <ul class="list-group">
      {{#each model as |item index|}}
        <a class="list-group-item" href="#">{{item}} {{index}}</a>
      {{/each}}
    </ul>
  </div>

  <div class="col-sm-9 col-md-10 main">
    {{outlet}}
    <h1>hello</h1>
  </div>
</div>
